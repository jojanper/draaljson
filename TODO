- environments should list target detectly instead of passing a path to a target:
  {
    "output": ...,
    "target": ...,
    "inputSchema": ...,
    "schemaDb": ...
  }

- make sure property type can be detected

- make separate function: const fieldType = property.type || this.schemaDb[property.$ref].type || 'unknown';
