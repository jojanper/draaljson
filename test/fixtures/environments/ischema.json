{
    "id": "/MasterLevel",
    "type": "object",
    "properties": {
      "version": {"type": "string"},
      "output": {"type": "string"},
      "targets": {
        "type": "array",
        "items": {"type": "string"}
      }
    },
    "required": ["version", "output", "targets"]
}
