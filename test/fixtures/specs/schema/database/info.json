{
    "id": "/info",
    "type": "object",
    "properties": {
      "package": {"type": "string"},
      "manual": {"type": "boolean"},
      "price": {"type": "integer"},
      "regions": {
        "type": "array",
        "items": {"type": "string"},
        "minItems": 1,
        "uniqueItems": true
      }
    },
    "required": ["price", "manual", "package", "regions"]
}
