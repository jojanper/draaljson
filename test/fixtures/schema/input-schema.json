{
    "id": "/MasterLevel",
    "type": "object",
    "properties": {
      "version": {"type": "string"},
      "output": {"type": "string"},
      "target": {
        "type": "object",
        "properties": {
          "inputSchema": {
            "type": "string",
            "minLength": 1
          },
          "targets": {
            "type": "array",
            "items": {"type": "string"},
            "minItems": 1,
            "uniqueItems": true
          }
        },
        "required": ["inputSchema", "targets"]
      }
    },
    "required": ["version", "output", "target"]
}
